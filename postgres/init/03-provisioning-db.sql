CREATE TABLE provisioning.client_sequence (
    client_sequence_id SERIAL PRIMARY KEY
);


CREATE TABLE provisioning.identity_provider_sequence (
    identity_provider_sequence_id SERIAL PRIMARY KEY
);


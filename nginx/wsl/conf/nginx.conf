events {
  worker_connections  1024;  ## Default: 1024
}

http {
  include /etc/nginx/proxy.conf;

  server { # simple reverse-proxy
    listen       443 ssl;
    server_name  wsl;
    ssl_certificate     /etc/ssl/certs/wsl.crt;
    ssl_certificate_key /etc/ssl/certs/wsl.key;

    location /iamcentral/ {
      proxy_pass       http://iamcentral:8080/;
    }

    location /iamshared/ {
      proxy_pass       http://iamshared:8080/;
    }
  }
}

